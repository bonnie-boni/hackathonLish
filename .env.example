# Example environment variables for ModernShop
# Copy this file to `.env.local` and fill with your project values.

# ── MongoDB ───────────────────────────────────────────────
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# ── M-Pesa Daraja API (Sandbox) ──────────────────────────
# For sandbox use the Safaricom sandbox URL. For production change to the live API URL below.
DARAJA_BASE_URL=https://sandbox.safaricom.co.ke
MPESA_CONSUMER_KEY=<your-daraja-consumer-key>
MPESA_CONSUMER_SECRET=<your-daraja-consumer-secret>
MPESA_SHORTCODE=174379
MPESA_PASSKEY=<your-daraja-passkey>
MPESA_CALLBACK_URL=https://your-public-domain.com/api/mpesa/callback

# Optional: a test phone number (Kenya format) used by developers
TEST_PHONE_NUMBER=2547XXXXXXXX

# For production use this base URL:
# DARAJA_BASE_URL=https://api.safaricom.co.ke

# ── Email / SMTP (optional, recommended) ─────────────────
# You can use SMTP (Gmail App Password or other SMTP provider). If SMTP is
# configured the server will use it to send receipts. Otherwise SendGrid can
# be used (set SENDGRID_API_KEY instead).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=xanensismo@gmail.com
SMTP_PASS="hchuokpqqcvrtntl"
SMTP_SECURE=false
RECEIPT_FROM_EMAIL=xanensismo@gmail.com

# Optional: SendGrid fallback (if you prefer SendGrid instead of SMTP)
# SENDGRID_API_KEY=<your-sendgrid-key>

# ── Other (optional) ─────────────────────────────────────
# Set to your app URL if needed by auth/email workflows
NEXTAUTH_URL=http://localhost:3000
